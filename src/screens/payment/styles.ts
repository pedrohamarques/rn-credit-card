import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 32,
        marginTop: 24,
    },
    button: {
        alignItems: "center",
        marginVertical: 32,
    },
});
